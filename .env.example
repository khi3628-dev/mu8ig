# Copy to .env.local (never commit .env.local)
# Required for /api/ai/* routes.
ANTHROPIC_API_KEY=sk-ant-...

# Set to "1" to make all AI routes return canned fixtures (dev without burning credits)
AI_MOCK=0
